<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin.mapper.WepsocketMapper">
	
	<select id="selectUserTypeByAdmin" parameterType="String" resultType="String">
	#관리자인지
		SELECT MEMBER_TYPE
		  FROM MEMBERS
		 WHERE MEMBER_ID = #{userId}
	</select>
	
</mapper>